{
	"name": "simplex-task",
	"version": "1.0.0",
	"description": "Simplex currency exchange widget NodeJS API",
	"main": "dist/index.js",
	"scripts": {
		"prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
		"build": "tsc",
		"prestart": "npm run build",
		"start": "node .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "benaszab@gmail.com",
	"license": "ISC",
	"dependencies": {
		"axios": "^0.21.1",
		"express": "^4.17.1",
		"joi": "^17.4.0"
	},
	"devDependencies": {
		"@types/express": "^4.17.12",
		"@types/node": "^16.0.0",
		"tslint": "^6.1.3",
		"typescript": "^4.3.5"
	}
}
